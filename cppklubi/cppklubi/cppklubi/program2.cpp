#include <iostream>
#include <stdint.h>

int main()
{
	/*
	std::cout << "syötä luku: ";

	int count = 0;
	std::cin >> count;

	int* elements = new int[count];

	std::cout << "allocated " << sizeof(int) * count << " bytes..." << std::endl;

	for (int i = 0; i < count; i++)
	{
		elements[i] = i + 1;
	}

	for (int i = 0; i < count; i++)
	{

		// TODO: tulosta kaikki kakkosella jaolliset luvut
		int mod = elements[i] % 2;

		if (mod == 0)
		{
			std::cout << elements[i] << std::endl;
		}
	}
	for (int i = 0; i < count; i++)
	{
		//Tulosta kaikki kolmosella jaolliset luvut
		int mod = elements[i] % 3;
		if (mod == 0)
		{
			std::cout << elements[i] << std::endl;
		}
	}
	int sum = 0;
	for (int i = 0; i < count; i++)
	{
		//Tulosta kaikkien lukujen summa
		sum += elements[i];
		
	}
	std::cout << "SUM: " << sum << std::endl;
		//std::cout << "INDEX: " << i << " - VALUE: " << elements[i] << std::endl;

	//delete [] vapauttaa muistin koko taulukon osalta
	delete[] elements;

	*/

	/*
	char ch;
	unsigned char uch;

	short sh;
	unsigned short ush;

	int i;
	unsigned int ui;

	long l;
	long long ll;
	unsigned long ul;
	unsigned long long ull;

	int8_t i8;
	uint8_t ui8;

	system("pause");

	return 0;
	*/
}